{
  "name": "project-passkey",
  "version": "1.0.0",
  "description": "Webauthn Passkey authentication for Tic Tac Toe game",
  "main": "Backend/Server.js",
  "scripts": {
    "setup": "npm ci && npm --prefix Backend ci && npm --prefix Frontend ci",
    "start": "npm run start-backend",
    "build": "npm --prefix Frontend run build",
    "deploy": "node build-and-deploy.js",
    "start-backend": "npm --prefix Backend start",
    "start:deployed-local": "npm run deploy && npm run start-backend",
    "heroku-postbuild": "npm --prefix Frontend install && npm --prefix Frontend run build && npm --prefix Backend install"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "keywords": [
    "webauthn",
    "passkey",
    "authentication"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "jwt-decode": "^4.0.0"
  }
}
